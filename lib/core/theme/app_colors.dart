import 'package:flutter/material.dart';

// Extracted from dwelleo.sa live CSS
abstract final class AppColors {
  // Brand primary — lime green
  static const Color primary = Color(0xFF8CC63F);
  static const Color primaryDark = Color(0xFF6BA82E);
  static const Color primaryLight = Color(0xFFB0D96B);

  // Brand accent — purple
  static const Color accent = Color(0xFF6B4FA0);
  static const Color accentLight = Color(0xFF9B7FD4);

  // Neutrals
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceDark = Color(0xFF1A1A2E);
  static const Color background = Color(0xFFF8F9FA);
  static const Color backgroundDark = Color(0xFF121212);

  // Text
  static const Color textPrimary = Color(0xFF1A1A2E);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Status
  static const Color success = Color(0xFF22C55E);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
  static const Color info = Color(0xFF3B82F6);

  // Property card
  static const Color cardBackground = Color(0xFFFFFFFF);
  static const Color divider = Color(0xFFE5E7EB);
  static const Color shadow = Color(0x1A000000);
}
