# UNVERIFIED — route/params/payload are GUESSED, not confirmed against the live API.
# Capture the real request from dwelleo.sa network traffic before trusting this. See docs/api/REAL_API_SPEC.md
$kind: http-request
name: Save Property
order: 2000
method: POST
url: '{{baseUrl}}/api/v1/saved'
description: Save (favorite) a property for the authenticated user.
headers:
  - key: Content-Type
    value: application/json
  - key: Accept
    value: application/json
  - key: Accept-Language
    value: '{{locale}}'
body:
  type: json
  content: |-
    {
      "property_id": "123"
    }
