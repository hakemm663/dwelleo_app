# UNVERIFIED — route/params/payload are GUESSED, not confirmed against the live API.
# Capture the real request from dwelleo.sa network traffic before trusting this. See docs/api/REAL_API_SPEC.md
$kind: http-request
name: AI Voice Search
order: 2000
method: POST
url: '{{baseUrl}}/api/v1/search/voice'
description: Perform an AI-powered property search from an uploaded audio file (multipart/form-data).
headers:
  - key: Accept
    value: application/json
  - key: Accept-Language
    value: '{{locale}}'
body:
  type: formdata
  content:
    - key: audio
      type: text
      value: ''
      description: Audio file upload (multipart). Replace with file in app.
