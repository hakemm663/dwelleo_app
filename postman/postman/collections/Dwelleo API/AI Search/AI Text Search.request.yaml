# UNVERIFIED — route/params/payload are GUESSED, not confirmed against the live API.
# Capture the real request from dwelleo.sa network traffic before trusting this. See docs/api/REAL_API_SPEC.md
$kind: http-request
name: AI Text Search
order: 1000
method: POST
url: '{{baseUrl}}/api/v1/search/ai'
description: Perform an AI-powered natural language property search using a text query.
headers:
  - key: Content-Type
    value: application/json
  - key: Accept
    value: application/json
  - key: Accept-Language
    value: '{{locale}}'
body:
  type: json
  content: |-
    {
      "query": "apartments in Riyadh with pool and gym"
    }
